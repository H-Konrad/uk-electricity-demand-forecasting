### Models

This folder contains trained ML models used in the project. 

Only the deployed model is included in the repository. Other trained models produced during experimentation are not included. 

The deployed model is used by the FastAPI backend to generate live electricity demand forecasts.